import 'package:flutter/material.dart';

class WouldYouRather extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Would you rather'),
      ),
      body: Center(
        child: Text('Question Goes Here'),
      ),
    );
  }
}
