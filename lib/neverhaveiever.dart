import 'package:flutter/material.dart';

class NeverHaveIEver extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Never Have I ever'),
      ),
      body: Center(
        child: Text('Question Goes Here'),
      ),
    );
  }
}
